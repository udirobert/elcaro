{
  "version": "v1",
  "hooks": [
    {
      "name": "Elcaro injection guard",
      "description": "Scans web-fetched content for indirect prompt injection. Fires after any web tool use. Uses the Elcaro miner API to score the content and warns if risk >= 0.5.",
      "trigger": "PostToolUse",
      "matcher": "web",
      "action": {
        "type": "command",
        "command": "python3 scripts/kiro-scan-hook.py",
        "timeout": 15
      }
    }
  ]
}
