import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Elcaro — Prompt Injection Detection",
  description:
    "Detect indirect prompt injection in content retrieved by AI agents. Scan emails, search results, documents, and web pages before your agent processes them.",
  openGraph: {
    title: "Elcaro — Prompt Injection Detection for AI Agents",
    description:
      "Protect your AI agents from indirect prompt injection. Scan content in real-time with a fast, rule-based detection engine.",
    type: "website",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased dark`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
